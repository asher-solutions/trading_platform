Frontend Project Source Folder - "assets"

This is where we work on files

***However

Django ONLY works with outputs generated by bundler (webpack) that is why
we dump the files somewhere Django finds them (see static/desc.txt)